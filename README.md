# FinFlow

Desktop finance manager built with Python and PySide6.

## Setup

```powershell
python -m venv venv
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
python main.py
```

## Build

```powershell
pyinstaller FinFlow.spec
```
